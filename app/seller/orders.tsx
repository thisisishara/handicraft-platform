import OrdersScreen from '../../screens/buyer/OrdersScreen';

// For now, reusing the same orders screen for sellers
// In a real app, you'd have separate seller order management
export default OrdersScreen;
